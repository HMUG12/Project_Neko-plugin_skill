# ============================================================
# 本仓库只保留：技能包本体 + 仓库说明文件
# 屏蔽其余一切（plugins/、_research/、assets/ 等）
# ============================================================

# 默认忽略仓库根下所有文件与目录
*

# —— 保留技能包本体 neko-plugin-dev(4.0.4) ——
!neko-plugin-dev(4.0.4)/
!neko-plugin-dev(4.0.4)/**

# —— 保留仓库说明文件 ——
!README.md
!CHANGELOG.md
!LICENSE

# —— 保留 .gitignore 自身 ——
!.gitignore
